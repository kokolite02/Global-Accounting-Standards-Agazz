{
  "name": "global-accounting-standards",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "engines": {
    "node": ">=18.17.0"
  },
  "scripts": {
    "dev": "pnpm --filter @gas/web dev",
    "build": "pnpm --filter @gas/web build",
    "lint": "pnpm --filter @gas/web lint",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}